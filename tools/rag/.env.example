# No API keys required — embeddings run locally via @xenova/transformers

# Optional: override which directory to index (defaults to repo root)
# REPO_ROOT=/absolute/path/to/your/repo

# Optional: custom Hugging Face model cache location
# HF_CACHE=/path/to/cache
